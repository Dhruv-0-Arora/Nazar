# runtime state (canonical home is ~/brain/, but never commit local copies)
runs/
inbox/

# python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# node / ui build
node_modules/
ui/dist/

# macOS
.DS_Store

# usb-transport per-device configs, generated by setup.sh / setup.ps1
collect*.conf
collect.conf.json
